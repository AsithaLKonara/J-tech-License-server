{
  "name": "upload-bridge-license-server",
  "version": "1.0.0",
  "description": "License server for Upload Bridge - Vercel deployment",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}

